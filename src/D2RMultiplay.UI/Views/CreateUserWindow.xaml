<Window x:Class="D2RMultiplay.UI.Views.CreateUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Windows User" Height="450" Width="400" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <StackPanel Margin="20">
        <TextBlock x:Name="TitleText" Text="Create New Windows User" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- Username -->
        <TextBlock x:Name="LabelUser" Text="Windows Username:" Margin="0,0,0,2"/>
        <TextBox x:Name="TxtUsername" Height="25" Margin="0,0,0,10"/>

        <!-- Password -->
        <TextBlock x:Name="LabelPass" Text="Password:" Margin="0,0,0,2"/>
        <PasswordBox x:Name="TxtPassword" Height="25" Margin="0,0,0,10"/>
        
        <!-- BattleTag -->
        <TextBlock x:Name="LabelBattleTag" Text="BattleTag (Optional):" Margin="0,0,0,2"/>
        <TextBox x:Name="TxtBattleTag" Height="25" Margin="0,0,0,10"/>

        <!-- Description/Note -->
        <TextBlock x:Name="LabelNote" Text="Description for User (Optional):" Margin="0,0,0,2"/>
        <TextBox x:Name="TxtNote" Height="25" Margin="0,0,0,20"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="BtnCreate" Content="Create" Width="100" Height="30" Margin="0,0,10,0" Click="BtnCreate_Click" IsDefault="True"/>
            <Button x:Name="BtnCancel" Content="Cancel" Width="80" Height="30" Click="BtnCancel_Click" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>
